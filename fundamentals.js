// array
const numbers = [48, 23, 53, 12, 99];
numbers[0] = 7;

const propertys = { name: 'riyad', class: '11', age: 22 };
const value = propertys['name'];
console.log(propertys[value]);


// loop
for (let i = 0; i < numbers.length; i++){
    const number = numbers[i];
    // console.log(number);
}

// function 
function multipel(num1, num2) {
    const sum = num1 + num2;
    return sum;
}
const result = multipel(15, 15);
// console.log(result);


